Array.splice(index,number_of_elements): notesObj.splice(index, 1) removes one element from the notesObj array at the specified index. It is commonly used when deleting an item from an array, which seems to be the purpose in the context of your deleteNote function.

In the context of the splice() method, the 1 represents the number of elements to be removed from the array starting at the specified index.